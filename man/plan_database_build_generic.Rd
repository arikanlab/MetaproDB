% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_database_build_generic.R
\name{plan_database_build_generic}
\alias{plan_database_build_generic}
\title{Plan a generic database build without a genome resource index}
\usage{
plan_database_build_generic(
  manifest_tbl,
  resource_dir,
  output_root = "results/database_builds",
  resource_mode = c("cache", "fresh")
)
}
\arguments{
\item{manifest_tbl}{Output of \code{build_genus_manifest()}.}

\item{resource_dir}{Directory containing genus genome zip files.}

\item{output_root}{Root output directory for database build products.}

\item{resource_mode}{Resource handling mode. \code{"cache"} uses only existing local
genome zip files and does not trigger downloads. \code{"fresh"} marks all genera
for fresh download regardless of existing local files.}
}
\value{
A tibble describing the planned generic database build.
}
\description{
Creates a workflow-ready build table from a genus manifest by constructing
expected genome zip paths and downstream output paths. This planner does not
require a pre-existing genus index and is intended for workflows where
Snakemake either uses only existing local archives (\code{"cache"}) or downloads
fresh genome archives for all selected genera (\code{"fresh"}).
}
